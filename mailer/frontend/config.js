// Cloud / same-origin defaults.
// When UI is served FROM the backend (Koyeb / PythonAnywhere), leave apiUrl empty.
// GitHub Pages users: set apiUrl to your Koyeb URL once, e.g. "https://mailer-xxxx.koyeb.app"
window.MAILER_CONFIG = {
  apiUrl: "",
  apiKey: "",
};
